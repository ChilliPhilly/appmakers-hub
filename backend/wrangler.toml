name = "qa-hub-sync"
main = "src/worker.js"
compatibility_date = "2026-07-01"

# The KV namespace that stores session result state.
# `wrangler kv namespace create QA_STATE` prints the id — paste it below.
[[kv_namespaces]]
binding = "QA_STATE"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
